<h1>Posts#show</h1>
<p>Find me in app/views/posts/show.html.erb</p>


 <%= @post.title %>
  <% @post.tags.each do |t| %>
  <%= t.name %>
<% end %>

<%= link_to "edit", edit_post_path(@post) %>
<%= link_to "delete", @post, method: :delete, data: {alert:"you sure?"}  %>